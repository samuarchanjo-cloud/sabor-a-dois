# Migração histórica do projeto original

O SQL desta pasta foi preservado apenas para auditoria histórica. Ele contém seeds e guardas específicos do banco original e **não deve ser executado** ao criar um cardápio novo.

Somente os arquivos em `supabase/migrations/` fazem parte da instalação reproduzível do template.
